<form method="post" action="{{ path(constant('App\\Controller\\RouteName::ORDER_CANCEL'), {'id': order.id}) }}" onsubmit="return confirm('Êtes vous sûrs de vouloir annuler cette commande ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order.id) }}">
    <button class="btn btn-danger mt-4 mb-4">Annuler la commande</button>
</form>